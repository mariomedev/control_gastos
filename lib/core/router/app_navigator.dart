import 'package:flutter/widgets.dart';

/// Global navigator key used across the app for navigation outside widget context.
final GlobalKey<NavigatorState> rootNavigatorKey = GlobalKey<NavigatorState>();
